{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Eudaimonia Frontend Project",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "type-check": "tsc --noEmit",
    "dev": "npm run dev --workspace=packages/app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/app",
    "packages/components",
    "packages/utils"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}